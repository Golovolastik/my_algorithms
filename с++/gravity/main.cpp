// description is in the 'descr.h' file
#include <vector>
#include <iostream>

std::vector<int> flip;

int main(){
    std::vector<int>&before;
    return 0;
};

std::vector<int> flip(const char dir, const std::vector<int>& arr) {
    return arr;
}
